machine:
  install:
    extraKernelArgs:
      - amd_pstate=passive  # lets the CPU use its own power management
  udev:
    rules:
      - ACTION=="add", SUBSYSTEM=="block", KERNEL=="sd[a-z]", RUN+="/sbin/hdparm -B 127 -S 241 /dev/%k"
